// value notifier
// value listenable

import 'package:flutter/material.dart';

ValueNotifier<int> currentPageNotifier = ValueNotifier(0);
ValueNotifier<bool> isDarkThemeActive = ValueNotifier(false);